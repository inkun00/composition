# 저장소 작업 원칙

## 파일 크기

- 사람이 작성하는 코드 파일은 한 파일당 물리적 1,000줄을 넘기지 않는다.
- 800줄에 도달하면 다음 기능을 추가하기 전에 역할별 모듈, 훅, 컴포넌트 또는 스타일 파일로 분리한다.
- 줄 수를 줄이기 위한 압축, 여러 문장의 한 줄 배치, 가독성을 해치는 축약은 금지한다.
- 자동 생성 파일은 예외로 하되 파일명에 `.generated.`를 포함하고 생성기와 원본 데이터를 함께 유지한다. 자동 생성 파일은 직접 수정하지 않는다.
- 현재 1,000줄을 넘는 레거시 파일은 `scripts/check-file-lines.mjs`의 동결 상한보다 한 줄도 늘릴 수 없다. 해당 파일을 수정할 때는 가능한 기능 단위를 새 파일로 옮겨 줄 수를 줄인다.
- 파일 분리만으로 순환 의존성, 거대한 props 객체 또는 중복 상태를 만들지 않는다. 상태는 소유자가 분명한 훅으로, 화면은 의미 있는 영역 컴포넌트로, 오디오 로직은 재생·녹음·렌더링 책임별 모듈로 나눈다.

`npm run check:file-lines`가 이 원칙을 검사하며 `npm test`와 `npm run build` 전에 자동 실행된다.
